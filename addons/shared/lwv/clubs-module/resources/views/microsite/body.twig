{% if website.web_body|length %}
    <section id="body" class="block block-text full_width">
        <div class="container">
            <div class="inner text-left">
                {{ website.web_body|raw }}
            </div>
        </div>
    </section>
{% endif %}