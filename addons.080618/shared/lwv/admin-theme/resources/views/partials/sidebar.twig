<aside id="sidebar" class="scrollbar">

    <ul>
        {% for key, navigation in template.cp.navigation %}
            <li class="{{ navigation.active ? 'active' }} variant-border">
                <a {{ html_attributes(navigation.attributes) }}>
                    {{ navigation.icon()|raw }}
                    <span>{{ trans(navigation.title) }}</span>
                </a>
            </li>
        {% endfor %}
    </ul>

</aside>
